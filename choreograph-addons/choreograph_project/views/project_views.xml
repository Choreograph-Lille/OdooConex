<odoo>
  <data>
    <!-- project task -->
    <record id="view_task_form2_inherit" model="ir.ui.view">
      <field name="name">project.task.form.inherit</field>
      <field name="model">project.task</field>
      <field name="inherit_id" ref="project.view_task_form2"/>
      <field name="arch" type="xml">
        <field name="user_ids" position="before">
          <field name="role_id"/>
        </field>
        <field name="description" position="before">
          <group>
            <field name="show_sms_coupling" invisible="1"/>
            <field name="sms_coupling" attrs="{'invisible': [('show_sms_coupling', '=', False)]}"/>
          </group>
          <group>
            <field name="show_phone_coupling" invisible="1"/>
            <field name="phone_coupling" attrs="{'invisible': [('show_phone_coupling', '=', False)]}"/>
          </group>
          <group>
            <field name="show_email_coupling" invisible="1"/>
            <field name="email_coupling" attrs="{'invisible': [('show_email_coupling', '=', False)]}"/>
          </group>
        </field>
      </field>
    </record>
    <record id="view_task_tree2_inherit" model="ir.ui.view">
      <field name="name">project.task.tree.inherit</field>
      <field name="model">project.task</field>
      <field name="inherit_id" ref="project.view_task_tree2"/>
      <field name="arch" type="xml">
        <field name="user_ids" position="before">
          <field name="role_id"/>
        </field>
      </field>
    </record>
  </data>
</odoo>