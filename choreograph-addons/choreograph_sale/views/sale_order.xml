<odoo>
    <!-- Form View -->
  <record id="view_order_form_inherit" model="ir.ui.view">
    <field name="name">sale.order.form.inherit</field>
    <field name="model">sale.order</field>
    <field name="inherit_id" ref="sale.view_order_form"/>
    <field name="type">form</field>
    <field name="arch" type="xml">
      <header position="inside">
        <field name="show_operation_generation_button" invisible="1"/>
        <field name="new_condition_count" invisible="1"/>
        <field name="project_count" invisible="1"/>
        <button name="action_generate_operation" id="action_generate_operation" string="Operation generation"
                class="btn-primary" type="object" attrs="{'invisible': [('show_operation_generation_button', '=', False)]}"/>
        <button name="action_create_task_from_condition" id="action_create_task_from_condition" string="Task generation"
                class="btn-primary" type="object" attrs="{'invisible': ['|', ('new_condition_count', '=', 0), ('project_count', '=', 0)]}"/>
      </header>
      <notebook position="inside">
        <page name="operation_condition" string="Condition/Exclusion">
          <field name="operation_condition_ids"/>
        </page>
      </notebook>
    </field>
  </record>
</odoo>