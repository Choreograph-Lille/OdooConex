<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        
        <record id="account_move_wizard_form_view" model="ir.ui.view">
            <field name="name">account.move.wizard.form</field>
            <field name="model">account.move.wizard</field>
            <field name="arch" type="xml">
                <form string="Account Move Wizard">
                    <sheet>
                        <group>
                            <field name="move_id" force_save="True"/>
                            <field name="note" placeholder="Justification..."/>
                        </group>
                    </sheet>
                    <footer>
                        <button string="Validate" type="object" name="button_validate" class="btn-primary"/>
                        <button string="Discard" class="btn-secondary" special="cancel" data-hotkey="z"/>
                    </footer>
                </form>
            </field>
        </record>
        
        <record id="account_move_wizard_act_window" model="ir.actions.act_window">
            <field name="name">Account Move Wizard</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">account.move.wizard</field>
            <field name="view_mode">form</field>
            <field name="context">{}</field>
            <field name="target">new</field>
        </record>
    
    </data>
</odoo>